services:
  - type: web
    name: mp404
    runtime: python
    buildCommand: pip install -r requirements.txt && pip install -U --pre "yt-dlp[default]"
    startCommand: gunicorn app:app --bind 0.0.0.0:$PORT
